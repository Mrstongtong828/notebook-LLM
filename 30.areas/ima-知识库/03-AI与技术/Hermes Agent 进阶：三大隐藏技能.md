Hermes Agent 进阶：三大隐藏技能来源：https://www.aivi.fyi/llms/hermes-agent-advanced方案一：Ollama 一键集成（免费云端模型）核心：Ollama 已内置 Hermes Agent，不需要单独部署。步骤：下载安装 Ollama选 Hermes Agent → 复制启动命令 → 终端执行选模型（后缀带 Cloud 的是云端免费模型，如 MiniMax M2.7）浏览器登录账号 → 连接设备Gateway 刷新 → 选 App 连接 → 进入终端特点：零配置、零本地资源、3 分钟上手方案二：Open WebUI（最佳交互体验）痛点：聊天软件里用 Hermes 不方便——不支持 Markdown、历史难管理。优势：像 ChatGPT 一样左侧保存会话完整 Markdown 渲染 + 流式输出代码块独立展示、可在线运行支持搜索历史、上传文件、引用知识库手机端局域网也能用部署：装 Open WebUI（pip/docker）编辑 ~/.hermes/config.yaml，加两个参数启用 API 服务 + 设密码hermes gateway restartOpen WebUI 启动 → localhost:8080管理员设置 → 连接 → URL 填 http://localhost:8642/v1，认证填密码手机端：局域网 http://<电脑IP>:8080 即可访问，ngrok 可穿透到公网。方案三：主副模型分工（省钱核心）思路：核心对话走贵模型，辅助任务走免费/便宜模型。配置要点主模型（config.yaml 的 model 块）：model:  api_key: env:MINIMAX_CN_API_KEY  base_url: https://api.minimaxi.com/anthropic   # 不带 /v1  default: MiniMax-M2.7  provider: minimax-cn副模型（auxiliary 块，全部走 Gemini 2.5 Flash）：auxiliary:  approval:        { provider: gemini, model: gemini-2.5-flash, timeout: 30 }  compression:     { provider: gemini, model: gemini-2.5-flash, timeout: 120 }  flush_memories:  { provider: gemini, model: gemini-2.5-flash, timeout: 30 }  mcp:             { provider: gemini, model: gemini-2.5-flash, timeout: 30 }  session_search:  { provider: gemini, model: gemini-2.5-flash, timeout: 30 }  skills_hub:      { provider: gemini, model: gemini-2.5-flash, timeout: 30 }  title_generation:{ provider: gemini, model: gemini-2.5-flash, timeout: 30 }  vision:          { provider: gemini, model: gemini-2.5-flash, timeout: 30 }  web_extract:     { provider: gemini, model: gemini-2.5-flash, timeout: 360 }compression 调优：compression:  enabled: true  protect_last_n: 20  target_ratio: 0.2  threshold: 0.5⚠️ 不要在 compression 块里放 summary_model，模型选择统一在 auxiliary.compression 配。环境变量（~/.hermes/.env）：MINIMAX_CN_API_KEYGOOGLE_API_KEYOPENAI_API_KEY自定义 provider 注意：api_mode: /anthropic → anthropic_messages，/v1 → chat_completionsname 不能和内置 provider 重名验证# YAML 语法python3 -c "import yaml; yaml.safe_load(open('$HOME/.hermes/config.yaml'))" && echo OK# 配置诊断hermes doctor# 功能测试hermes chat -q "Say exactly 'pong' and nothing else." -Q回滚：cp ~/.hermes/config.yaml.bak-<时间戳> ~/.hermes/config.yaml